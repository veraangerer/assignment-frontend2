import { polycularTruncate } from '../../../helpers/polycular-truncate';
import { module, test } from 'qunit';

module('Unit | Helper | polycular truncate');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = polycularTruncate(42);
  assert.ok(result);
});
